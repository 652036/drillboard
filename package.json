{
  "name": "drillboard-webmcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A human-agent tabletop exercise simulator built for WebMCP.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node scripts/verify.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=20" }
}
